@{
    ViewBag.Title = "Entry Removed";
}

@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title</h1>
            </hgroup>
        </div>
    </section>
}

@Html.ActionLink("Back to list", "AddShippingInfoList") <br />
@Html.ActionLink("Back to index", "Index")